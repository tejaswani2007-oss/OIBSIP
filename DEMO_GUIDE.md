# Internship Demo Guide

Name: **Tejaswani Kommoju**  
Track: **Python Programming**

Record one short screen video for each completed task. Keep the title card visible for at least two seconds at the beginning.

## Demo 1: BMI Calculator

Title card:

```text
Tejaswani Kommoju
Python Programming
BMI Calculator
```

Run from the `OIBSIP` folder:

```powershell
python "Python-Task2-BMICalculator\bmi_calculator.py"
```

Show:

1. Valid input: `70` kg and `1.75` m.
2. The result `BMI: 22.86` and `Normal weight`.
3. Invalid input such as `abc` or `-5`.
4. The validation message and repeat option.

Suggested LinkedIn caption:

```text
I completed a BMI Calculator project during my Python Programming internship at Oasis Infobyte. The project validates user input, calculates BMI, classifies the result, and supports repeated calculations.

#oasisinfobyte #python #internship
```

## Demo 2: Random Password Generator

Title card:

```text
Tejaswani Kommoju
Python Programming
Random Password Generator
```

Run:

```powershell
python "Python-Task3-PasswordGenerator\password_generator.py"
```

Show a 16-character password using uppercase, lowercase, numbers, and symbols. Show the strength result, then demonstrate an invalid length such as `7`.

Suggested LinkedIn caption:

```text
I completed a secure Random Password Generator during my Python Programming internship at Oasis Infobyte. It uses Python secrets, enforces password rules, guarantees selected character types, and reports password strength.

#oasisinfobyte #python #internship #cybersecurity
```

## Demo 3: Voice Assistant

Title card:

```text
Tejaswani Kommoju
Python Programming
Voice Assistant
```

Run:

```powershell
python "Python-Task1-VoiceAssistant\voice_assistant.py"
```

Show these commands:

```text
hello
time
date
help
exit
```

If microphone access is unavailable, type the commands. Mention that the assistant supports typed fallback, web search, weather, reminders, general knowledge, and YouTube lookup.

Suggested LinkedIn caption:

```text
I completed a Voice Assistant project during my Python Programming internship at Oasis Infobyte. It supports voice input with typed fallback, greetings, time and date, searches, weather, reminders, knowledge answers, and YouTube lookup.

#oasisinfobyte #python #internship #voiceassistant
```

## Final Submission

GitHub repository:

https://github.com/tejaswani2007-oss/OIBSIP

Submit this link through the official OIBSIP submission form after posting the demo videos and commenting meaningfully on two other interns' videos.
